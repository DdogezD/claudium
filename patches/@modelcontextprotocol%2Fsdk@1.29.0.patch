diff --git a/dist/cjs/client/stdio.js b/dist/cjs/client/stdio.js
index 1dd822e1012f85015f6ec274e5828dfa5b24e777..e84efbcc0ee83740d434e4747ff01f8b3e8dcafe 100644
--- a/dist/cjs/client/stdio.js
+++ b/dist/cjs/client/stdio.js
@@ -56,6 +56,7 @@ class StdioClientTransport {
     constructor(server) {
         this._readBuffer = new stdio_js_1.ReadBuffer();
         this._stderrStream = null;
+        this._fatalFrameError = false;
         this._serverParams = server;
         if (server.stderr === 'pipe' || server.stderr === 'overlapped') {
             this._stderrStream = new node_stream_1.PassThrough();
@@ -94,8 +95,23 @@ class StdioClientTransport {
             this._process.stdin?.on('error', error => {
                 this.onerror?.(error);
             });
+            // Patched: detect oversized frames before they accumulate unboundedly.
             this._process.stdout?.on('data', chunk => {
-                this._readBuffer.append(chunk);
+                try {
+                    this._readBuffer.append(chunk);
+                }
+                catch (error) {
+                    if (error && typeof error === 'object' && error.code === 'FRAME_TOO_LARGE') {
+                        if (this._fatalFrameError)
+                            return;
+                        this._fatalFrameError = true;
+                        this.onerror?.(error);
+                        void this.close().catch(() => {});
+                        return;
+                    }
+                    this.onerror?.(error);
+                    return;
+                }
                 this.processReadBuffer();
             });
             this._process.stdout?.on('error', error => {
diff --git a/dist/cjs/server/stdio.js b/dist/cjs/server/stdio.js
index 0edcbf325fc4c40826c621c598abf55d64da5ba5..aaed2aeb823ad5f5f8c0446d00cd9e8cf2b91c7b 100644
--- a/dist/cjs/server/stdio.js
+++ b/dist/cjs/server/stdio.js
@@ -17,9 +17,24 @@ class StdioServerTransport {
         this._stdout = _stdout;
         this._readBuffer = new stdio_js_1.ReadBuffer();
         this._started = false;
+        this._fatalFrameError = false;
         // Arrow functions to bind `this` properly, while maintaining function identity.
         this._ondata = (chunk) => {
-            this._readBuffer.append(chunk);
+            try {
+                this._readBuffer.append(chunk);
+            }
+            catch (error) {
+                if (!this._started || this._fatalFrameError)
+                    return;
+                if (error && typeof error === 'object' && error.code === 'FRAME_TOO_LARGE') {
+                    this._fatalFrameError = true;
+                    this.onerror?.(error);
+                    void this.close().catch(() => {});
+                    return;
+                }
+                this.onerror?.(error);
+                return;
+            }
             this.processReadBuffer();
         };
         this._onerror = (error) => {
diff --git a/dist/cjs/shared/stdio.js b/dist/cjs/shared/stdio.js
index 540ee568277cdd1285262010421c4436cbae26e9..18b92146937c88f9fe6ef447b0ba8e6133b0b949 100644
--- a/dist/cjs/shared/stdio.js
+++ b/dist/cjs/shared/stdio.js
@@ -4,11 +4,46 @@ exports.ReadBuffer = void 0;
 exports.deserializeMessage = deserializeMessage;
 exports.serializeMessage = serializeMessage;
 const types_js_1 = require("../types.js");
+/**
+ * Maximum wire-frame size for a single JSON-RPC / NDJSON message (64 MiB).
+ * Matches the existing stdio stderr cap and accommodates common MCP image /
+ * resource blobs while preventing unbounded accumulation from absent-newline
+ * output.
+ */
+const MAX_MESSAGE_SIZE = 64 * 1024 * 1024;
 /**
  * Buffers a continuous stdio stream into discrete JSON-RPC messages.
  */
 class ReadBuffer {
     append(chunk) {
+        let pending = 0;
+        if (this._buffer) {
+            const lastNL = this._buffer.lastIndexOf('\n');
+            pending = lastNL === -1 ? this._buffer.byteLength : this._buffer.byteLength - (lastNL + 1);
+        }
+        let start = 0;
+        while (true) {
+            const idx = chunk.indexOf('\n', start);
+            if (idx === -1) {
+                const segmentLen = pending + (chunk.byteLength - start);
+                if (segmentLen > MAX_MESSAGE_SIZE) {
+                    this._buffer = undefined;
+                    const err = new Error('MCP message exceeds 64 MiB size limit');
+                    err.code = 'FRAME_TOO_LARGE';
+                    throw err;
+                }
+                break;
+            }
+            const segmentLen = pending + (idx - start + 1);
+            if (segmentLen > MAX_MESSAGE_SIZE) {
+                this._buffer = undefined;
+                const err = new Error('MCP message exceeds 64 MiB size limit');
+                err.code = 'FRAME_TOO_LARGE';
+                throw err;
+            }
+            pending = 0;
+            start = idx + 1;
+        }
         this._buffer = this._buffer ? Buffer.concat([this._buffer, chunk]) : chunk;
     }
     readMessage() {
diff --git a/dist/esm/client/stdio.js b/dist/esm/client/stdio.js
index 3118783c5d4e8c65dfb2e555293f89ce6d67bb6e..b0bd5e3f96bf87ace2a6bf1923ff108fd13f0007 100644
--- a/dist/esm/client/stdio.js
+++ b/dist/esm/client/stdio.js
@@ -49,6 +49,7 @@ export class StdioClientTransport {
     constructor(server) {
         this._readBuffer = new ReadBuffer();
         this._stderrStream = null;
+        this._fatalFrameError = false;
         this._serverParams = server;
         if (server.stderr === 'pipe' || server.stderr === 'overlapped') {
             this._stderrStream = new PassThrough();
@@ -87,8 +88,23 @@ export class StdioClientTransport {
             this._process.stdin?.on('error', error => {
                 this.onerror?.(error);
             });
+            // Patched: detect oversized frames before they accumulate unboundedly.
             this._process.stdout?.on('data', chunk => {
-                this._readBuffer.append(chunk);
+                try {
+                    this._readBuffer.append(chunk);
+                }
+                catch (error) {
+                    if (error && typeof error === 'object' && error.code === 'FRAME_TOO_LARGE') {
+                        if (this._fatalFrameError)
+                            return;
+                        this._fatalFrameError = true;
+                        this.onerror?.(error);
+                        void this.close().catch(() => {});
+                        return;
+                    }
+                    this.onerror?.(error);
+                    return;
+                }
                 this.processReadBuffer();
             });
             this._process.stdout?.on('error', error => {
diff --git a/dist/esm/server/stdio.js b/dist/esm/server/stdio.js
index 727fe70a5575e44e10d3224d4e01d39ce2faf4cc..1c8e7c81f13472b08c4d2959a5c99ba20c1bd072 100644
--- a/dist/esm/server/stdio.js
+++ b/dist/esm/server/stdio.js
@@ -11,9 +11,24 @@ export class StdioServerTransport {
         this._stdout = _stdout;
         this._readBuffer = new ReadBuffer();
         this._started = false;
+        this._fatalFrameError = false;
         // Arrow functions to bind `this` properly, while maintaining function identity.
         this._ondata = (chunk) => {
-            this._readBuffer.append(chunk);
+            try {
+                this._readBuffer.append(chunk);
+            }
+            catch (error) {
+                if (!this._started || this._fatalFrameError)
+                    return;
+                if (error && typeof error === 'object' && error.code === 'FRAME_TOO_LARGE') {
+                    this._fatalFrameError = true;
+                    this.onerror?.(error);
+                    void this.close().catch(() => {});
+                    return;
+                }
+                this.onerror?.(error);
+                return;
+            }
             this.processReadBuffer();
         };
         this._onerror = (error) => {
diff --git a/dist/esm/shared/stdio.js b/dist/esm/shared/stdio.js
index 30f299f5a7c096444c10ca4bfc7645944d1392e1..35e2c74c7b6d5362fcf34ac3fa1dd78e8e919f3c 100644
--- a/dist/esm/shared/stdio.js
+++ b/dist/esm/shared/stdio.js
@@ -1,9 +1,50 @@
 import { JSONRPCMessageSchema } from '../types.js';
+/**
+ * Maximum wire-frame size for a single JSON-RPC / NDJSON message (64 MiB).
+ * Matches the existing stdio stderr cap and accommodates common MCP image /
+ * resource blobs while preventing unbounded accumulation from absent-newline
+ * output.
+ */
+const MAX_MESSAGE_SIZE = 64 * 1024 * 1024;
 /**
  * Buffers a continuous stdio stream into discrete JSON-RPC messages.
  */
 export class ReadBuffer {
     append(chunk) {
+        // Determine how many trailing bytes (after the last newline) are
+        // already buffered.  Those bytes belong to the same logical frame as
+        // the beginning of the incoming chunk.
+        let pending = 0;
+        if (this._buffer) {
+            const lastNL = this._buffer.lastIndexOf('\n');
+            pending = lastNL === -1 ? this._buffer.byteLength : this._buffer.byteLength - (lastNL + 1);
+        }
+        let start = 0;
+        while (true) {
+            const idx = chunk.indexOf('\n', start);
+            if (idx === -1) {
+                // Trailing segment — could span additional chunks.
+                const segmentLen = pending + (chunk.byteLength - start);
+                if (segmentLen > MAX_MESSAGE_SIZE) {
+                    this._buffer = undefined;
+                    const err = new Error('MCP message exceeds 64 MiB size limit');
+                    err.code = 'FRAME_TOO_LARGE';
+                    throw err;
+                }
+                break;
+            }
+            // Complete frame: pending prefix + chunk bytes up to and including '\n'.
+            const segmentLen = pending + (idx - start + 1);
+            if (segmentLen > MAX_MESSAGE_SIZE) {
+                this._buffer = undefined;
+                const err = new Error('MCP message exceeds 64 MiB size limit');
+                err.code = 'FRAME_TOO_LARGE';
+                throw err;
+            }
+            pending = 0; // frame ended — reset for next frame
+            start = idx + 1;
+        }
+        // Safe: every frame segment is ≤ MAX_MESSAGE_SIZE.
         this._buffer = this._buffer ? Buffer.concat([this._buffer, chunk]) : chunk;
     }
     readMessage() {
