/**
 * Legacy model migrations are intentionally disabled.
 * Existing model settings remain readable and are no longer rewritten to an
 * Anthropic family alias or silently remapped to another model.
 */
export function migrateLegacyOpusToCurrent(): void {}
